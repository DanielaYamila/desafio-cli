const ItemListContainer = (promp) => {
    return (
        <div>
            <img src="#" alt="" />
            <h2>{promp.name}</h2>
            <p>{promp.propiedad}</p>
            <p>{promp.valor}</p>
        </div>
    );
}

export default ItemListContainer;